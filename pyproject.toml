[project]
name = "matchgenomeipl"
version = "0.1.0"
description = "First vertical slice for IPL ingestion, analytics, and pre-delivery state reconstruction"
requires-python = ">=3.10"
readme = "README.md"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

